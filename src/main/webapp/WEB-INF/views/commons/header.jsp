<%@ page language="java" contentType="text/html; charset=UTF-8"   pageEncoding="UTF-8"%>
 <%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>
 <!-- 
 
<div id="loginMenu">
	<c:choose>
		<c:when test="${sessionScope.loginId != null && !notiChk}">
			<strong>${sessionScope.loginId}</strong> 님 <a href="#">마이페이지</a> <span>|</span><img id="notix" src="resources/photo/noti.png"  alt="noti" /> <span>|</span> <a href="logout.do">로그아웃</a>
		</c:when>
		<c:when test="${sessionScope.loginId != null && notiChk}">
			<strong>${sessionScope.loginId}</strong> 님 <a href="#">마이페이지</a> <span>|</span><img id="notix" src="resources/photo/noti4.png" alt="noti" /> <span>|</span> <a href="logout.do">로그아웃</a>
		</c:when>
		<c:otherwise>
			<a href="/login.go">로그인</a>
		</c:otherwise>
	</c:choose>
	<div id="notiList">
		<table id="notiContent">
		</table>
	</div>
</div>
  -->
 